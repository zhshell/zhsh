# 变更记录

本项目遵循语义化版本。

## 0.1.0 - 2026-09-08

### 安全

- Agent 命令的解析身份、安全判断、用户确认和执行绑定为同一不可变计划。
- 保持默认 `balanced` 策略，同时把副作用风险与数据披露风险分别判断。
- 非法 `HOME` 进入无用户持久状态的降级模式，不再从当前目录加载私有状态。
- Agent 拒绝后台/脱离语法，并有界监督命令进程组和输出管道。
- Codec 运行时按单项隔离故障，发布门禁继续严格拒绝任何损坏制品。

### 分发

- 首个公开 prototype 的官方包支持范围固定为 Ubuntu 24.04 LTS、amd64、glibc 2.39、Bash 5。
- `.deb`、RPM 和源码安装统一使用 `/usr` 布局；官方 Codec 全部使用 SDK 0.3.0/payload v3，
  并收束为 OpenAI/Anthropic 各一个 `0.3.0`；`JSON_SCHEMA=off|on` 在同一制品内选择请求 Profile。
- 增加固定容器、依赖审计、Debian 静态检查、全新 HOME 安装后冒烟和签名 tag 门禁。
